/**
 * Export all API services
 */
import api, { authAPI, productAPI, categoryAPI, subcategoryAPI, uploadAPI } from './api';

export {
  api as default,
  authAPI,
  productAPI,
  categoryAPI,
  subcategoryAPI,
  uploadAPI
};
